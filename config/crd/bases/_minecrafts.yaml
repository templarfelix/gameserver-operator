---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: minecrafts.
spec:
  group: ""
  names:
    kind: Minecraft
    listKind: MinecraftList
    plural: minecrafts
    singular: minecraft
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: Minecraft is the Schema for the minecrafts API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MinecraftSpec defines the desired state of Minecraft
            properties:
              config:
                description: MinecraftConfig defines configuration for Minecraft &
                  LinuxGSM
                properties:
                  gsm:
                    description: LinuxGSM specific configuration
                    properties:
                      configFile:
                        description: Custom LinuxGSM configuration file content
                        type: string
                    type: object
                  jvm:
                    description: Java virtual machine configuration
                    properties:
                      extraArgs:
                        description: Additional JVM arguments
                        type: string
                      maxHeapSize:
                        default: 2G
                        description: 'Maximum heap size (default: "2G")'
                        type: string
                      minHeapSize:
                        default: 1G
                        description: 'Minimum heap size (default: "1G")'
                        type: string
                    type: object
                  plugins:
                    description: Plugins configuration (for Spigot/Paper/Pukkit servers)
                    properties:
                      configMaps:
                        description: Custom plugin configuration files as ConfigMap/Secret
                          references
                        items:
                          type: string
                        type: array
                      install:
                        description: List of plugins to install from Spiget.org or
                          direct URLs
                        items:
                          description: MinecraftPlugin defines a plugin to install
                          properties:
                            enable:
                              default: true
                              description: Whether to enable the plugin
                              type: boolean
                            name:
                              description: Plugin name (for known plugins)
                              type: string
                            spigetId:
                              description: Plugin ID from Spiget.org (if using Spiget)
                              format: int32
                              type: integer
                            url:
                              description: Direct download URL (alternative to Spiget)
                              type: string
                            version:
                              description: Plugin version (if specific version required)
                              type: string
                          type: object
                        type: array
                    type: object
                  serverProperties:
                    description: Server properties configuration
                    properties:
                      allowEnd:
                        default: true
                        description: Whether to allow end
                        type: boolean
                      allowNether:
                        default: true
                        description: Whether to allow nether
                        type: boolean
                      difficulty:
                        default: normal
                        description: Difficulty level (peaceful, easy, normal, hard)
                        enum:
                        - peaceful
                        - easy
                        - normal
                        - hard
                        type: string
                      enableCommandBlocks:
                        default: false
                        description: Whether to enable command blocks
                        type: boolean
                      enforceWhitelist:
                        default: false
                        description: Whether to enforce whitelist
                        type: boolean
                      gameMode:
                        default: survival
                        description: Game mode (survival, creative, adventure, spectator)
                        enum:
                        - survival
                        - creative
                        - adventure
                        - spectator
                        type: string
                      levelSeed:
                        description: Level seed
                        type: string
                      levelType:
                        description: Level type (default, flat, largebiomes, amplified,
                          buffet)
                        enum:
                        - default
                        - flat
                        - largebiomes
                        - amplified
                        - buffet
                        type: string
                      maxPlayers:
                        default: 20
                        description: Maximum number of players
                        format: int32
                        type: integer
                      motd:
                        description: Motd (message of the day)
                        type: string
                      onlineMode:
                        default: true
                        description: Whether to require online-mode authentication
                        type: boolean
                      pvp:
                        default: true
                        description: Whether to allow PvP
                        type: boolean
                      resourcePack:
                        description: Resource pack URL
                        type: string
                      resourcePackSha1:
                        description: Resource pack SHA-1 hash
                        type: string
                      serverIcon:
                        description: Server icon (base64 encoded image)
                        type: string
                      serverPort:
                        default: 25565
                        description: 'Server port (default: 25565)'
                        format: int32
                        type: integer
                      simulationDistance:
                        default: 10
                        description: Simulation distance (Minecraft 1.18+)
                        format: int32
                        type: integer
                      viewDistance:
                        default: 10
                        description: View distance
                        format: int32
                        type: integer
                    type: object
                  serverType:
                    default: vanilla
                    description: Server type (vanilla, spigot, paper, bukkit, forge,
                      fabric)
                    enum:
                    - vanilla
                    - spigot
                    - paper
                    - bukkit
                    - forge
                    - fabric
                    type: string
                type: object
              image:
                default: gameservermanagers/gameserver:minecraft
                type: string
            required:
            - image
            type: object
          status:
            description: MinecraftStatus defines the observed state of Minecraft
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
