---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: dayzs.
spec:
  group: ""
  names:
    kind: Dayz
    listKind: DayzList
    plural: dayzs
    singular: dayz
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: Dayz is the Schema for the dayzs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DayzSpec defines the desired state of Dayz
            properties:
              config:
                description: DayzConfig defines configuration for Dayz & LinuxGSM
                properties:
                  game:
                    description: Game server configuration (server.cfg)
                    properties:
                      adminPassword:
                        default: adminpassword
                        description: Admin password for server administration
                        type: string
                      allowedBuild:
                        default: ""
                        description: Allowed build mismatch version
                        type: string
                      battlEye:
                        default: true
                        description: Enable BattlEye
                        type: boolean
                      disable3rdPerson:
                        default: false
                        description: Disable third person view
                        type: boolean
                      disableAI:
                        description: Disable AI gathering around players
                        format: int32
                        type: integer
                      disableAutoGroup:
                        description: Disable automatic server gathering
                        type: boolean
                      disableCrosshair:
                        default: false
                        description: Disable crosshair
                        type: boolean
                      disableVoIP:
                        default: true
                        description: Enable server VOIP
                        type: boolean
                      enableAI:
                        default: false
                        description: Enable AI
                        type: boolean
                      forceSameBuild:
                        default: true
                        description: Force same build
                        type: boolean
                      forceVoiceCodec:
                        default: 0
                        description: Force voice codec
                        format: int32
                        type: integer
                      hostname:
                        default: DayZ Server
                        description: Server hostname
                        type: string
                      limitFPS:
                        default: 30
                        description: Limit FPS
                        format: int32
                        type: integer
                      maxAI:
                        default: 100
                        description: Max AI count
                        format: int32
                        type: integer
                      maxPlayers:
                        default: 60
                        description: Maximum number of players
                        format: int32
                        type: integer
                      password:
                        description: Server password (blank = no password)
                        type: string
                      persistencyDisabled:
                        description: Enable protection for persisted data
                        type: boolean
                      requiredBuild:
                        description: Required build
                        format: int32
                        type: integer
                      respawnTime:
                        default: 300
                        description: Respawn time
                        format: int32
                        type: integer
                      serverNightTimeAcceleration:
                        default: "4"
                        description: Server night time acceleration
                        type: string
                      serverTime:
                        default: 8
                        description: Server time
                        type: string
                      serverTimeAcceleration:
                        default: "12"
                        description: Server time acceleration
                        type: string
                      serverTimePersistent:
                        default: true
                        description: Server time persistent
                        type: boolean
                      showStreamStatistics:
                        description: Show stream statistics
                        type: boolean
                      stats:
                        default: true
                        description: Allow statistics
                        type: boolean
                      statsIP:
                        description: Statistics IP
                        type: string
                      statsPassword:
                        description: Statistics password
                        type: string
                      statsPort:
                        default: 2302
                        description: Statistics port
                        format: int32
                        type: integer
                      steamQueryPort:
                        default: 60
                        description: Login timeout
                        format: int32
                        type: integer
                      verifySignatures:
                        default: true
                        description: Verify signatures
                        type: boolean
                      von:
                        default: true
                        description: Enable voice over IP on the server
                        format: int32
                        type: integer
                    type: object
                  gsm:
                    description: LinuxGSM specific configuration
                type: object
              image:
                default: gameservermanagers/gameserver:dayz
                type: string
            required:
            - image
            type: object
          status:
            description: DayzStatus defines the observed state of Dayz
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
