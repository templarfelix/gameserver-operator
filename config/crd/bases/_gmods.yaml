---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: gmods.
spec:
  group: ""
  names:
    kind: Gmod
    listKind: GmodList
    plural: gmods
    singular: gmod
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: Gmod is the Schema for the gmods API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GmodSpec defines the desired state of Gmod
            properties:
              config:
                description: GmodConfig defines configuration for Garry's Mod & LinuxGSM
                properties:
                  game:
                    description: Game server configuration (server.cfg)
                    properties:
                      adminSteamIds:
                        description: Admin SteamID64 (comma-separated for multiple
                          admins)
                        type: string
                      ads:
                        default: false
                        description: Enable/disable custom admin messages
                        type: boolean
                      allowCSLua:
                        default: true
                        description: Allow custom CSS file loading
                        type: boolean
                      allowEffects:
                        default: true
                        description: Enable/disable effects spawning
                        type: boolean
                      allowNPCs:
                        default: true
                        description: Enable/disable NPC spawning
                        type: boolean
                      allowProps:
                        default: true
                        description: Enable/disable props spawning
                        type: boolean
                      allowRagdolls:
                        default: true
                        description: Enable/disable ragdolls spawning
                        type: boolean
                      allowVehicles:
                        default: true
                        description: Enable/disable vehicle spawning
                        type: boolean
                      allowWeapons:
                        default: true
                        description: Enable/disable weapons spawning
                        type: boolean
                      alltalk:
                        default: true
                        description: Dead players can hear live players
                        type: boolean
                      darkRpEconomy:
                        default: false
                        description: Enable/disable darkRP economy system
                        type: boolean
                      defaultMap:
                        default: gm_construct
                        description: Default map
                        type: string
                      fallDamage:
                        default: true
                        description: Enable/disable fall damage
                        type: boolean
                      fastDlUrl:
                        description: Fast download URL for workshop items
                        type: string
                      gameMode:
                        default: sandbox
                        description: Game mode
                        type: string
                      hostname:
                        default: Garry's Mod Server
                        description: Server hostname
                        type: string
                      lanOnly:
                        default: 0
                        description: Enable LAN only (0=Internet, 1=LAN only)
                        format: int32
                        type: integer
                      logFile:
                        default: false
                        description: Enable/disable prop deletion logging
                        type: boolean
                      loggingLevel:
                        default: 1
                        description: Logging level (1=minimal, 2=normal, 3=verbose)
                        format: int32
                        type: integer
                      m9kWeapons:
                        default: false
                        description: Enable/disable M9K weapons
                        type: boolean
                      mapCycle:
                        default: true
                        description: Enable/disable map cycling
                        type: boolean
                      mapCycleFile:
                        default: mapcycle.txt
                        description: Map cycle file (relative path)
                        type: string
                      maxPlayers:
                        default: 16
                        description: Maximum number of players
                        format: int32
                        type: integer
                      maxPropsPerPlayer:
                        default: 200
                        description: Maximum number of props per player
                        format: int32
                        type: integer
                      motd:
                        description: MOTD file URL or text
                        type: string
                      password:
                        description: Server password (blank = no password)
                        type: string
                      pvp:
                        default: true
                        description: Enable/disable PvP
                        type: boolean
                      region:
                        default: 255
                        description: Server region (0=US East, 1=US West, 2=South
                          America, 3=Europe, 4=Asia, 5=Australia, 6=Middle East, 7=Africa,
                          255=World)
                        format: int32
                        type: integer
                      serverDescription:
                        default: A Garry's Mod Server
                        description: Server description
                        type: string
                      serverOwnerSteamId:
                        description: Server owner SteamID64
                        type: string
                      serverTags:
                        description: Server tags (comma-separated)
                        type: string
                      voiceDistance:
                        default: 0
                        description: Voice distance in units
                        format: int32
                        type: integer
                      voiceIcon:
                        default: true
                        description: Voice icon display
                        type: boolean
                      workshopCollectionId:
                        description: Workshop collection ID for addons
                        type: string
                    type: object
                  gsm:
                    description: LinuxGSM specific configuration
                    properties:
                      clientPort:
                        default: 27005
                        description: Client port (used for connection)
                        format: int32
                        type: integer
                      configFile:
                        description: Custom LinuxGSM configuration file content
                        type: string
                      gslt:
                        description: Game Login Token (GSLT) for public servers
                        type: string
                      port:
                        default: 27015
                        description: Server port
                        format: int32
                        type: integer
                      sourceTvPort:
                        default: 27020
                        description: SourceTV port
                        format: int32
                        type: integer
                      stats:
                        default: false
                        description: Enable/disable LinuxGSM stats reporting
                        type: boolean
                      tickRate:
                        default: 66
                        description: Server tickrate
                        format: int32
                        type: integer
                      workshopCollectionId:
                        description: Workshop collection ID (alternative to server.cfg)
                        type: string
                    type: object
                type: object
              image:
                default: gameservermanagers/gameserver:gmod
                type: string
            required:
            - image
            type: object
          status:
            description: GmodStatus defines the observed state of Gmod
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
