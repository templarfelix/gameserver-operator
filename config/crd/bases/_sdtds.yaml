---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: sdtds.
spec:
  group: ""
  names:
    kind: Sdtd
    listKind: SdtdList
    plural: sdtds
    singular: sdtd
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: Sdtd is the Schema for the sdtds API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SdtdSpec defines the desired state of Sdtd
            properties:
              config:
                description: SdtdConfig defines configuration for 7 Days to Die &
                  LinuxGSM
                properties:
                  game:
                    description: Game server configuration (serverconfig.xml)
                    properties:
                      adminFileName:
                        default: serveradmin.xml
                        description: Admin file name
                        type: string
                      airDropFrequency:
                        default: 72
                        description: Air drop frequency (hours)
                        format: int32
                        type: integer
                      airDropMarker:
                        default: true
                        description: Air drop marker
                        type: boolean
                      bedrollDeadZoneSize:
                        default: 15
                        description: Bedroll deadzone size
                        format: int32
                        type: integer
                      bedrollExpiryTime:
                        default: 45
                        description: Bedroll expiry time in days
                        format: int32
                        type: integer
                      blockDamageAI:
                        default: 100
                        description: Block damage by AI (%)
                        format: int32
                        type: integer
                      blockDamageAIBM:
                        default: 100
                        description: Block damage by AI during blood moon (%)
                        format: int32
                        type: integer
                      blockDamagePlayer:
                        default: 100
                        description: Block damage by players (%)
                        format: int32
                        type: integer
                      bloodMoonEnemyCount:
                        default: 8
                        description: Blood moon enemy count per player
                        format: int32
                        type: integer
                      bloodMoonFrequency:
                        default: 7
                        description: Blood moon frequency (days, 0=none)
                        format: int32
                        type: integer
                      bloodMoonRange:
                        default: 0
                        description: Blood moon range (days deviation)
                        format: int32
                        type: integer
                      bloodMoonWarning:
                        default: 8
                        description: Blood moon warning hour
                        format: int32
                        type: integer
                      buildCreate:
                        default: false
                        description: Cheat mode
                        type: boolean
                      dayLightLength:
                        default: 18
                        description: Game hours of sunlight per day
                        format: int32
                        type: integer
                      dayNightLength:
                        default: 60
                        description: Real time minutes per game day
                        format: int32
                        type: integer
                      dropOnDeath:
                        default: 1
                        description: Drop on death (0=nothing, 1=everything, 2=toolbelt,
                          3=backpack, 4=delete all)
                        format: int32
                        type: integer
                      dropOnQuit:
                        default: 0
                        description: Drop on quit (0=nothing, 1=everything, 2=toolbelt,
                          3=backpack)
                        format: int32
                        type: integer
                      dynamicMeshEnabled:
                        default: true
                        description: Dynamic mesh system
                        type: boolean
                      dynamicMeshLandClaimBuffer:
                        default: 3
                        description: Dynamic mesh land claim buffer
                        format: int32
                        type: integer
                      dynamicMeshLandClaimOnly:
                        default: true
                        description: Dynamic mesh only in land claims
                        type: boolean
                      dynamicMeshMaxItemCache:
                        default: 3
                        description: Dynamic mesh max item cache
                        format: int32
                        type: integer
                      eacEnabled:
                        default: true
                        description: Technical settings
                        type: boolean
                      enableMapRendering:
                        default: false
                        description: Enable map rendering for dashboard
                        type: boolean
                      enemyDifficulty:
                        default: 0
                        description: Enemy difficulty (0=Normal, 1=Feral)
                        format: int32
                        type: integer
                      enemySpawnMode:
                        default: true
                        description: Zombie settings
                        type: boolean
                      gameDifficulty:
                        default: 1
                        description: Difficulty
                        format: int32
                        type: integer
                      gameMode:
                        default: GameModeSurvival
                        description: GameModeSurvival
                        type: string
                      gameName:
                        description: Game name affects save game and seed
                        type: string
                      gameWorld:
                        default: Navezgane
                        description: GAMEPLAY - World
                        type: string
                      gameWorldSeed:
                        description: World generation seed (for RWG worlds)
                        type: string
                      gameWorldSize:
                        default: 6144
                        description: World generation size
                        format: int32
                        type: integer
                      hideCommandExecutionLog:
                        default: 0
                        description: Hide command execution log (0=all, 1=hide from
                          telnet, 2=hide from clients, 3=hide all)
                        format: int32
                        type: integer
                      landClaimCount:
                        default: 3
                        description: Land claim options
                        format: int32
                        type: integer
                      landClaimDeadZone:
                        default: 30
                        description: Land claim deadzone distance
                        format: int32
                        type: integer
                      landClaimDecayMode:
                        default: 0
                        description: Land claim decay mode (0=Slow, 1=Fast, 2=None)
                        format: int32
                        type: integer
                      landClaimExpiryTime:
                        default: 7
                        description: Land claim expiry time (days)
                        format: int32
                        type: integer
                      landClaimOfflineDelay:
                        default: 0
                        description: Land claim offline delay (minutes)
                        format: int32
                        type: integer
                      landClaimOfflineDurabilityModifier:
                        default: 4
                        description: Land claim offline durability modifier
                        format: int32
                        type: integer
                      landClaimOnlineDurabilityModifier:
                        default: 4
                        description: Land claim online durability modifier
                        format: int32
                        type: integer
                      landClaimSize:
                        default: 41
                        description: Land claim size in blocks
                        format: int32
                        type: integer
                      language:
                        default: English
                        description: Primary language for players on this server
                        type: string
                      lootAbundance:
                        default: 100
                        description: Loot settings
                        format: int32
                        type: integer
                      lootRespawnDays:
                        default: 7
                        description: Loot respawn days
                        format: int32
                        type: integer
                      maxChunkAge:
                        default: -1
                        description: Chunk age limit in days (-1=disabled)
                        format: int32
                        type: integer
                      maxQueuedMeshLayers:
                        default: 1000
                        description: Max queued mesh layers
                        format: int32
                        type: integer
                      maxSpawnedAnimals:
                        default: 50
                        description: Animal limit
                        format: int32
                        type: integer
                      maxSpawnedZombies:
                        default: 64
                        description: Performance - Zombie limits
                        format: int32
                        type: integer
                      maxUncoveredMapChunksPerPlayer:
                        default: 131072
                        description: Max uncovered map chunks per player
                        format: int32
                        type: integer
                      partySharedKillRange:
                        default: 100
                        description: Multiplayer
                        format: int32
                        type: integer
                      persistentPlayerProfiles:
                        default: false
                        description: Persistent player profiles
                        type: boolean
                      playerKillingMode:
                        default: 3
                        description: Player killing mode (0=No Killing, 1=Allies Only,
                          2=Strangers Only, 3=Everyone)
                        format: int32
                        type: integer
                      playerSafeZoneHours:
                        default: 5
                        description: Player safe zone hours
                        format: int32
                        type: integer
                      playerSafeZoneLevel:
                        default: 5
                        description: Player safe zone level
                        format: int32
                        type: integer
                      region:
                        default: NorthAmericaEast
                        description: The region this server is in
                        enum:
                        - NorthAmericaEast
                        - NorthAmericaWest
                        - CentralAmerica
                        - SouthAmerica
                        - Europe
                        - Russia
                        - Asia
                        - MiddleEast
                        - Africa
                        - Oceania
                        type: string
                      saveDataLimit:
                        default: -1
                        description: Save data limit in MB (-1=disabled)
                        format: int32
                        type: integer
                      serverAdminSlots:
                        default: 0
                        description: Admin slots (can join when server full)
                        format: int32
                        type: integer
                      serverAdminSlotsPermission:
                        default: 0
                        description: Required permission level for admin slots
                        format: int32
                        type: integer
                      serverDescription:
                        default: A 7 Days to Die server
                        description: Server description shown in server browser
                        type: string
                      serverDisabledNetworkProtocols:
                        default: SteamNetworking
                        description: Networking protocols to disable
                        type: string
                      serverLoginConfirmationText:
                        description: Server login confirmation text
                        type: string
                      serverMaxAllowedViewDistance:
                        default: 12
                        description: Max allowed view distance
                        format: int32
                        type: integer
                      serverMaxPlayerCount:
                        default: 8
                        description: Slots
                        format: int32
                        type: integer
                      serverMaxWorldTransferSpeedKiBs:
                        default: 512
                        description: Maximum world transfer speed in KiB/s
                        format: int32
                        type: integer
                      serverName:
                        default: 7 Days to Die Server
                        description: Server representation
                        type: string
                      serverPassword:
                        description: Password to gain entry to the server
                        type: string
                      serverPort:
                        default: 26900
                        description: Networking
                        format: int32
                        type: integer
                      serverReservedSlots:
                        default: 0
                        description: Reserved slots count (out of MaxPlayerCount)
                        format: int32
                        type: integer
                      serverReservedSlotsPermission:
                        default: 100
                        description: Required permission level to use reserved slots
                        format: int32
                        type: integer
                      serverVisibility:
                        default: 2
                        description: Server visibility (2=public, 1=friends only,
                          0=unlisted)
                        format: int32
                        type: integer
                      serverWebsiteURL:
                        description: Server website URL
                        type: string
                      telnetEnabled:
                        default: true
                        description: Telnet
                        type: boolean
                      telnetFailedLoginLimit:
                        default: 10
                        description: Telnet failed login limit before blocking
                        format: int32
                        type: integer
                      telnetFailedLoginsBlocktime:
                        default: 10
                        description: Telnet block duration in seconds
                        format: int32
                        type: integer
                      telnetPassword:
                        description: Telnet password
                        type: string
                      telnetPort:
                        default: 8081
                        description: Telnet port
                        format: int32
                        type: integer
                      terminalWindowEnabled:
                        default: true
                        description: Terminal window (Windows only)
                        type: boolean
                      twitchBloodMoonAllowed:
                        default: false
                        description: Allow twitch actions during blood moon
                        type: boolean
                      twitchServerPermission:
                        default: 90
                        description: Twitch integration permission level
                        format: int32
                        type: integer
                      webDashboardEnabled:
                        default: false
                        description: Admin interfaces - Web Dashboard
                        type: boolean
                      webDashboardPort:
                        default: 8080
                        description: Port of the web dashboard
                        format: int32
                        type: integer
                      webDashboardUrl:
                        description: External URL for web dashboard
                        type: string
                      xpMultiplier:
                        default: 100
                        description: XP multiplier (%)
                        format: int32
                        type: integer
                      zombieBMMove:
                        default: 3
                        description: Zombie blood moon movement
                        format: int32
                        type: integer
                      zombieFeralMove:
                        default: 3
                        description: Zombie feral movement
                        format: int32
                        type: integer
                      zombieFeralSense:
                        default: 0
                        description: Zombie feral sense (0=Off, 1=Day, 2=Night, 3=All)
                        format: int32
                        type: integer
                      zombieMove:
                        default: 0
                        description: Zombie movement (0=walk, 1=jog, 2=run, 3=sprint,
                          4=nightmare)
                        format: int32
                        type: integer
                      zombieMoveNight:
                        default: 3
                        description: Zombie movement at night
                        format: int32
                        type: integer
                    type: object
                  gsm:
                    description: LinuxGSM specific configuration
                type: object
              image:
                default: gameservermanagers/gameserver:7dtd
                type: string
            required:
            - image
            type: object
          status:
            description: SdtdStatus defines the observed state of Sdtd
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
