---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: projectzomboids.
spec:
  group: ""
  names:
    kind: ProjectZomboid
    listKind: ProjectZomboidList
    plural: projectzomboids
    singular: projectzomboid
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: ProjectZomboid is the Schema for the projectzomboids API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectZomboidSpec defines the desired state of ProjectZomboid
            properties:
              config:
                description: ProjectZomboidConfig defines configuration for Project
                  Zomboid & LinuxGSM
                properties:
                  game:
                    description: Game server configuration (server.ini)
                    properties:
                      adminPassword:
                        default: adminpassword
                        description: Admin password
                        type: string
                      adminSafehouseCooldown:
                        default: "2"
                        description: Admin safehouse cooldown
                        type: string
                      allowDestructionBySledgehammer:
                        default: true
                        description: Enable punching
                        type: boolean
                      bannedPlayerKickedTime:
                        default: 1
                        description: Ban kick time
                        format: int32
                        type: integer
                      bleedingChance:
                        default: "100"
                        description: Bleeding chance (0-100)
                        type: string
                      chatStreams:
                        default: 0
                        description: Chat streams
                        format: int32
                        type: integer
                      constructionPreRequisites:
                        default: true
                        description: Construction pre-requisites
                        type: boolean
                      damageMultiplier:
                        default: "3"
                        description: Damage multiplier
                        type: string
                      defaultPort:
                        default: 16261
                        description: Port for the server
                        format: int32
                        type: integer
                      dropOffWhiteListedObjects:
                        description: Drop off white-listed objects when player dies
                        type: boolean
                      fastForwardMultiplier:
                        default: 1
                        description: Fast forward multiplier (advanced)
                        format: int32
                        type: integer
                      foodRotSpeed:
                        default: "1.0"
                        description: Food rot speed multiplier
                        type: string
                      globalChat:
                        default: false
                        description: Enable global chat
                        type: boolean
                      hoursForLootRespawn:
                        default: "0.0"
                        description: Hours for loot respawn
                        type: string
                      map:
                        default: Muldraugh, KY
                        description: Map (what world generator to use)
                        enum:
                        - Muldraugh, KY
                        type: string
                      maxAccountsPerUser:
                        default: 0
                        description: Maximum accounts per user
                        format: int32
                        type: integer
                      maxItemsForLootRespawn:
                        default: "4"
                        description: Max items for loot respawn
                        type: string
                      maxPlayers:
                        default: 32
                        description: Maximum players
                        format: int32
                        type: integer
                      minutesPerDay:
                        default: "1.0"
                        description: Minutes surviving bonus multiplier
                        type: string
                      minutesPerPage:
                        default: "1.0"
                        description: Minutes per page (for reading books etc)
                        type: string
                      mods:
                        description: Mods (comma-separated mod names)
                        type: string
                      nutrition:
                        default: true
                        description: Nutrition enabled
                        type: boolean
                      openWhitelistMod:
                        default: server
                        description: Open whitelist mod
                        type: string
                      password:
                        description: Password for joining the server
                        type: string
                      pauseOnEmptyServer:
                        default: false
                        description: Pause when no players online
                        type: boolean
                      pingLimit:
                        default: 400
                        description: Ping limit
                        format: int32
                        type: integer
                      playerRespawnWithOther:
                        default: 0
                        description: Player respawn time in hours
                        format: int32
                        type: integer
                      playerRespawnWithSelf:
                        default: 0
                        description: Player respawn time in days
                        format: int32
                        type: integer
                      playerSafehouseCooldown:
                        default: "2"
                        description: Player safehouse cooldown
                        type: string
                      pvp:
                        default: true
                        description: Enable PVP
                        type: boolean
                      removeExpiredZombies:
                        default: false
                        description: Remove expire zombie
                        type: boolean
                      resetId:
                        description: Reset ID - increments by 1 every time server
                          configuration is reset
                        format: int32
                        type: integer
                      safehouseAllowDestroy:
                        default: false
                        description: Allow non-members to destroy safehouses
                        type: boolean
                      safehouseAllowRespawn:
                        default: true
                        description: Safehouse allow respawn
                        type: boolean
                      safehouseAllowTrepass:
                        default: false
                        description: Safehouse allow non-members claim
                        type: boolean
                      safehouseDaySurvivor:
                        default: "0"
                        description: Safehouse day survived to claim
                        type: string
                      saveWorldEveryMinutes:
                        default: 15
                        description: Save interval (minutes)
                        format: int32
                        type: integer
                      serverDescription:
                        default: A Project Zomboid Server
                        description: Server description
                        type: string
                      serverName:
                        default: Project Zomboid Server
                        description: Server name
                        type: string
                      serverPlayerID:
                        description: Server player ID
                        format: int32
                        type: integer
                      serverWelcomeMessage:
                        description: Server welcome message
                        type: string
                      sleepAllowed:
                        default: true
                        description: Sleep allowed
                        type: boolean
                      udpPort:
                        default: 16262
                        description: UDP port for the server
                        format: int32
                        type: integer
                      workshopItems:
                        description: Workshop items (comma-separated Steam workshop
                          IDs)
                        type: string
                      worldEraseSpeed:
                        default: 90
                        description: World erase speed
                        format: int32
                        type: integer
                      zombieLureDistance:
                        default: "0.0"
                        description: Zombie lure distance
                        type: string
                      zombieLureInterval:
                        default: 0
                        description: Zombie lure interval
                        format: int32
                        type: integer
                      zombieMigrateDistance:
                        default: 1000
                        description: Zombie migrate distance
                        format: int32
                        type: integer
                      zombiePopulation:
                        default: 3
                        description: Zombie population
                        format: int32
                        type: integer
                      zombieRespawnPeriod:
                        default: 1000
                        description: Zombies respawn period
                        format: int32
                        type: integer
                      zombieRespawnRate:
                        default: -1
                        description: Zombies respawn rate
                        format: int32
                        type: integer
                    type: object
                  gsm:
                    description: LinuxGSM specific configuration
                type: object
              image:
                default: gameservermanagers/gameserver:project-zomboid
                type: string
            required:
            - image
            type: object
          status:
            description: ProjectZomboidStatus defines the observed state of ProjectZomboid
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
